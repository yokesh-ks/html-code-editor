# Live HTML Editor

This project is an Astro based web application providing a real time HTML editor with live preview.

## Development

1. Install dependencies with your preferred package manager.
2. Run `npm run dev` to start the development server.

